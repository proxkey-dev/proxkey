# Archive

These documents are historical notes from earlier implementation passes.

They are kept for context only and may reference files, routes, or directory layouts that no longer exist in the current codebase.
